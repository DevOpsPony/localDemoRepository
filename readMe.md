# Testing pushing a locally made repository into github

Using SSH keys sure went wrong so i'll be testing this with https.
If this commit was pushed... it means things are good so far.
It'll be great if i could keep using https instead of ssh for this. 